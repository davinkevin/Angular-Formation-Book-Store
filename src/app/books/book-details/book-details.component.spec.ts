import { TestBed, async } from '@angular/core/testing';

import { AppComponent } from './book-details.component';

describe('AppComponent', () => {
  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [
        AppComponent
      ],
    }).compileComponents();
  }));

});
